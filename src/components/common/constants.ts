export const RENDERER_DEFAULTS = {
  radius: 20,
  baseAttrs: {
    fill: "transparent",
    stroke: "#212121",
    strokeWidth: 1,
  }
};

export const TREE_LAYOUT_DEFAULTS = {
  nodeDistance: 90,
  levelDistance: 100,
  nodeRadius: 20
};

export enum EDITOR_MODES {
  DRAWING,
  SIMULATION
}
